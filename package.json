{
  "name": "bank-expense-analyzer",
  "private": true,
  "version": "1.0.0",
  "description": "Bank statement analyzer: rule-based categorization first, Claude only for the ambiguous leftovers",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared",
    "dev": "npm run build:shared && concurrently -n server,web -c blue,green \"npm run start:dev -w apps/server\" \"npm run dev -w apps/web\"",
    "build": "npm run build:shared && npm run build -w apps/server && npm run build -w apps/web",
    "test": "npm run build:shared && npm test -w apps/server && npm test -w apps/web",
    "sample": "npm run build:shared && npm run sample -w apps/server",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.6.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
